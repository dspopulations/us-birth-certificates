[build-system]
requires = ["hatchling>=1.28"]
build-backend = "hatchling.build"

[project]
name = "dspopulations-us-birth-certificates"
dynamic = ["version"]
description = "Analysis of births of babies with Down syndrome recorded in US birth certificates."
readme = "README.md"
requires-python = ">=3.14"
license = { text = "AGPL-3.0-or-later" }
authors = [{ name = "Frank Buckley" }]

dependencies = [
  "numpy>=2.4.3",
  "pandas>=3.0.1",
  "duckdb>=1.5.0",
  "preliz>=0.18.0",
  "pymc>=5.28.1,<6.0",
  "nutpie>=0.16.8",
  "jaxlib>=0.9.2",
  "numpyro>=0.20.0",
  "arviz>=0.23.4,<1.0",
  "matplotlib>=3.10.8",
  "scipy>=1.17.1",
]

[project.optional-dependencies]
dev = [
  "notebook>=7.5.5",
  "ipython>=9.11.0",
  "ipywidgets>=8.1.8",
  "pytest>=9.0.2",
  "ruff>=0.15.5",
]

[tool.hatch.version]
path = "src/dspopulations-us-birth-certificates/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/dspopulations-us-birth-certificates"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.jupytext]
formats = "ipynb,py:percent"

[tool.ruff]
target-version = "py314"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = [
    "E501",  # line too long — long lines acceptable in scientific/modelling code
    "E741",  # ambiguous variable name — mathematical names (l, X, y) are meaningful
]

[tool.ruff.lint.isort]
known-first-party = ["dspopulations-us-birth-certificates"]
