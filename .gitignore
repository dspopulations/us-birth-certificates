/node_modules
/previous/us-birth-certificates/output
/notebooks/output
*.ipynb
# Data we must not redistribute (NCHS DUA) or that is large/binary.
# The data/ directory itself is tracked so derived CSVs can be committed.
*.pdf
*.parquet
*.db
*.sas7bdat
# Collaborators' working spreadsheets. Extract them to aggregate CSVs instead
# (see scripts/extract_degraaf_surveillance.py); use `git add -f` to track one
# deliberately.
/data/*.xlsx
__pycache__/
*.pyc
/.vscode
/output
/logs
/.claude
.DS_Store
/notes/__*
